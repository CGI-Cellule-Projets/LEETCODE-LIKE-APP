{
  "name": "algoforge",
  "private": true,
  "scripts": {
    "dev:full": "npm --prefix apps/editor run build && node apps/server/server.mjs",
    "build": "npm --prefix apps/editor run build",
    "serve": "node apps/server/server.mjs"
  }
}
